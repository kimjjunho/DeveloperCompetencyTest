
import 'package:flutter/cupertino.dart';

class DecidePromiseScreenScreen extends StatelessWidget {
  const DecidePromiseScreenScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
