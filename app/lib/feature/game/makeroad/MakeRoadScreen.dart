
import 'package:flutter/cupertino.dart';

class MakeRoadScreen extends StatelessWidget {
  const MakeRoadScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
