
import 'package:flutter/cupertino.dart';

class RotationFigureScreen extends StatelessWidget {
  const RotationFigureScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
