
import 'package:flutter/cupertino.dart';

class MakePotionScreen extends StatelessWidget {
  const MakePotionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
